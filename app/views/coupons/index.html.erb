<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Coupons</title>
  </head>
  <body>
    <% @coupons.each do |coupon| %>
    <h3><%= "Coupon #{coupon.id}" %></h3>
    <label>Code:</label><%= coupon.coupon_code %><br>
    <label>Store:</label><%= coupon.store %><br>
    <%= link_to "FREESTUFF", coupon_path(coupon) %>
    <br>
    <% end %>
  </body>
</html>
